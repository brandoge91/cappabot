module.exports.execute = async (client, message, args) => {
    message.reply("Support Server Coming Soon!")
    console.log("Command Used")
  }
  module.exports.help = {
      name: "support",
      aliases: ["s"],
      usage: `support`
  }